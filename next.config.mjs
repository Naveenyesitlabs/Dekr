// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: "export",
// };

// export default nextConfig;
// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   // output: "export", // ❌ REMOVE or comment this to enable API routes
//   // reactStrictMode: true, // ✅ Optional, but recommended
// };

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true
};

export default nextConfig;
